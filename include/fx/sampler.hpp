#ifndef KUEKEN_FX_SAMPLER_INCLUDED
#define KUEKEN_FX_SAMPLER_INCLUDED

#include "../core/renderer.hpp"

namespace fx{
namespace sampler
{
	//kueken::sampler::name load(kueken::renderer & Renderer, void * Pointer);
	//kueken::sampler::name load(kueken::renderer & Renderer, std::string const & Filename);

	//class command : public kueken::detail::command
	//{
	//public:
	//	command();
	//	command(
	//		kueken::renderer* Renderer,
	//		kueken::sampler::name Sampler);
	//	virtual void exec();

	//private:
	//	kueken::renderer* Renderer;
	//	kueken::sampler::name Sampler;
	//};

}//namespace sampler
}//namespace fx

#include "sampler.inl"

#endif//KUEKEN_FX_SAMPLER_INCLUDED

