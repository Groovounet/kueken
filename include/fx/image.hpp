#ifndef KUEKEN_FX_IMAGE_INCLUDED
#define KUEKEN_FX_IMAGE_INCLUDED

#include "../core/renderer.hpp"

namespace fx{
namespace image
{
	//kueken::image::name load(kueken::renderer & Renderer, void * Pointer);
	//kueken::image::name load(kueken::renderer & Renderer, std::string const & Filename);

	//class command : public kueken::detail::command
	//{
	//public:
	//	command();
	//	command(
	//		kueken::renderer* Renderer,
	//		kueken::sampler::name Sampler);
	//	virtual void exec();

	//private:
	//	kueken::renderer* Renderer;
	//	kueken::sampler::name Sampler;
	//};

}//namespace image
}//namespace fx

#include "image.inl"

#endif//KUEKEN_FX_IMAGE_INCLUDED


